{
  "scripts": {
    "dev": "live-server --port=8080",
    "deploy": "git add . && git commit -m \"chore: deploy\" && git push"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
